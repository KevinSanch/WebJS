const name = document.getElementsById(nameForm).value;
const email = document.getElementsById(emailForm).value;
const message = document.getElementsById(messageForm).value;

/*if (name.trim() === "") {

}
}*/

// Quería implementar mensajes de alerta y demás, pero sinceramente no me alcanzo el tiempo ya que estaba cursando otra cosa al mismo tiempo.
